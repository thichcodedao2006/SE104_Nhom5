<UserControl x:Class="QLTB.UserControlFolder.Device.DeviceFormView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLTB.UserControlFolder.Device"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             Background="Transparent">

    <Border Width="650" Background="{StaticResource MainBackgroundBrush}" CornerRadius="16" Padding="25">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Direction="-90" RenderingBias="Quality" ShadowDepth="2" Opacity="0.12"/>
        </Border.Effect>

        <StackPanel>
            <TextBlock Text="Cập nhật kho thiết bị" FontSize="22" FontWeight="Bold" Foreground="#111827" Margin="0,0,0,15"/>

            <GroupBox Header="1. THÔNG TIN CHUNG THIẾT BỊ " FontSize="13" FontWeight="Bold" Foreground="#2563EB" Margin="0,0,0,15" Padding="10,10,10,15">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Tên thiết bị" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource RoundedTextBox}" Height="38" Padding="10,0" VerticalContentAlignment="Center" Tag="Ví dụ: Máy tính Dell Latitude" Text="{Binding Name, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Nhà sản xuất" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource RoundedTextBox}" Height="38" Padding="10,0" VerticalContentAlignment="Center" Tag="Ví dụ: Dell Global" Text="{Binding Manufacturer, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>

                    <TextBlock Text="Loại thiết bị" FontWeight="SemiBold" Foreground="#374151" Margin="0,12,0,6"/>
                    <TextBox Style="{StaticResource RoundedTextBox}" Height="38" Padding="10,0" VerticalContentAlignment="Center" Tag="Ví dụ: Thiết bị điện tử, Thiết bị văn phòng..." Text="{Binding Category, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                </StackPanel>
            </GroupBox>

            <GroupBox Header="2. THÔNG TIN ĐỊNH DANH CHI TIẾT " FontSize="13" FontWeight="Bold" Foreground="#059669" Margin="0,0,0,10" Padding="10,10,10,15">
                <StackPanel>

                    <Grid Margin="0,0,0,12">
                        <TextBlock Text="Cách 1: Nhập thủ công một thiết bị đơn lẻ" VerticalAlignment="Center" FontStyle="Italic" Foreground="#6B7280"/>
                        <Button Content="Cách 2: Nhập nhanh hàng loạt từ file CSV" HorizontalAlignment="Right" Width="240" Height="32" Background="#ECFDF5" Foreground="#059669" FontWeight="SemiBold" BorderBrush="#A7F3D0" Command="{Binding ImportFileCommand}" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="6"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Số Serial" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource RoundedTextBox}" Height="38" Padding="10,0" VerticalContentAlignment="Center" Tag="Nhập số mã số định danh" Text="{Binding Serial, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                            <TextBlock Text="Trạng thái ban đầu" FontWeight="SemiBold" Foreground="#374151" Margin="0,12,0,6"/>
                            <ComboBox Style="{StaticResource ModernComboBox}" Height="38" SelectedValue="{Binding Status, Mode=TwoWay}" SelectedValuePath="Content">
                                <ComboBoxItem Content="Đang hoạt động"/>
                                <ComboBoxItem Content="Đang bảo trì"/>
                                <ComboBoxItem Content="Ngừng hoạt động"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Tên Phòng Ban phân bổ" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,6"/>
                            <TextBox Style="{StaticResource RoundedTextBox}" Height="38" Padding="10,0" VerticalContentAlignment="Center" Tag="Nhập tên phòng chính xác" Text="{Binding Department, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </GroupBox>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
                <Button Content="Hủy bỏ" Width="100" Height="38" Background="White" BorderBrush="#D1D5DB" Margin="0,0,12,0" Cursor="Hand" Command="{Binding CancelCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
                <Button Content="Xác nhận Lưu" Width="130" Height="38" Background="#2563EB" Foreground="White" FontWeight="Bold" BorderThickness="0" Cursor="Hand" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="8"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>