<UserControl x:Class="QLTB.UserControlFolder.Setting.SettingMainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:QLTB.UserControlFolder.Setting"
             xmlns:fa="http://schemas.fontawesome.io/icons/"
             xmlns:vm="clr-namespace:QLTB.ViewModel"
             xmlns:view="clr-namespace:QLTB.UserControlFolder.Setting"
             mc:Ignorable="d" 
             Background="#F8FAFC">
    <UserControl.Resources>

        <DataTemplate DataType="{x:Type vm:SettingProfileViewModel}">
            <view:SettingProfileView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SettingSecurityViewModel}">
            <view:SettingSecurityView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SettingNotificationViewModel}">
            <view:SettingNotificationView/>
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:SettingSystemViewModel}">
            <view:SettingSystemView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="28">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="24"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        

        <Border  
            BorderBrush="#E5E7EB" 
            BorderThickness="0,0,0,1"
            >
            <StackPanel Orientation="Horizontal">
                <RadioButton Content="Hồ sơ" 
                         FontSize="18"
                         IsChecked="True"
                         GroupName="SettingTabs"
                         Style="{StaticResource SidebarRadioButton}"
                         Command="{Binding ShowProfileCommand}"/>
                <RadioButton Content="Bảo mật" 
                         FontSize="18"
                         GroupName="SettingTabs"
                         Style="{StaticResource SidebarRadioButton}"
                         Command="{Binding ShowSecurityCommand}"
                         Margin="24,0,0,0"/>
                <RadioButton Content="Thông báo" 
                         FontSize="18"
                         GroupName="SettingTabs"
                         Style="{StaticResource SidebarRadioButton}"
                         Command="{Binding ShowNotificationCommand}"
                         Margin="24,0,0,0"/>
                <RadioButton Content="Hệ thống" 
                         FontSize="18"
                         GroupName="SettingTabs"
                         Style="{StaticResource SidebarRadioButton}"
                         Command="{Binding ShowSystemCommand}"
                         Margin="24,0,0,0"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="2"
        Background="White"
        BorderBrush="#E5E7EB"
        BorderThickness="1"
        CornerRadius="14"
        Padding="32">

            <ContentControl Content="{Binding CurrentView, NotifyOnTargetUpdated=True}">

                <ContentControl.RenderTransform>
                    <TranslateTransform />
                </ContentControl.RenderTransform>

                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Style.Triggers>
                            <EventTrigger RoutedEvent="Binding.TargetUpdated">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                 From="0" To="1" Duration="0:0:0.3"/>

                                        <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                 From="30" To="0" Duration="0:0:0.3">
                                            <DoubleAnimation.EasingFunction>
                                                <QuadraticEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>

            </ContentControl>
        </Border>
    </Grid>
</UserControl>